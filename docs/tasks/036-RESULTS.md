# Task 036 implementation results

> Status: complete; the Task 033 authority audit was recorded before removal,
> one reusable 21-file physical closure remains, and all final gates pass.

## Bound task and baseline

- Task contract SHA-256:
  `4bdd4b60fff8cc28c7f21a17763a949e2e123cafea84e2aad30252338be501f0`.
- Proposal revision 0.2 SHA-256:
  `be4636f2550481bd45cb982fe47c01ee8fbe0de74621f22e9d27cffca680cf7a`.
- Local `main`, fetched `origin/main`, and the task baseline were all
  `2b0180a47f7ac03011f56e9683c77060c8080f09` at activation.
- Governance remained inactive with Task 033 Phase 3 the next unactivated
  candidate. Task 036 made no routing change and consumed no Phase 3
  allocation.
- The authenticated Ksoloti checkout was at exact commit
  `08d3e6e1e2b61230308c20a15ded58ffdaf4656c`; its
  `firmware/mutable_instruments` subtree was clean.

Inherited Tide Pit UI-model/scope changes were present before Task 036 and were
preserved. They make the live focused/JUCE suites 6/8 tests rather than the
contract's tracked-baseline 5/7 counts; the exact DSP comparator remains the
decisive unchanged result.

## Task 033 authority audit

The user required an additional audit before removal of Tide Pit's duplicate
source. The previously recoverably moved subtree was restored byte-exact first.
At audit time both the Tide Pit-local tree and the shared physical package each
contained the same 21 regular files.

### Source release remains authoritative

`contracts/task033/phase2/source-release-05.json` remains the sole upstream
authority:

- reference: `schuss-source-release-000005@1`;
- content hash:
  `sha256:51750a00f07f98c783cfc1972580c9399ac64690eaf3d40ad6e1d736698c972c`;
- record-file SHA-256:
  `40f2a37b25c878c5eef31e0ea80394f2f0e02e019b2369656cc0412e377e83e1`;
- release scope: `patcher-source`;
- support claim: `source-identity-and-provenance-only`;
- importer boundary: `inventory-candidate-only`; and
- license and distribution review: `unreviewed` / `not-reviewed`.

That accepted record owns repository identity, commit, evidence anchors,
provenance, license-review state, and distribution-review state. Task 036 did
not edit it, its schema, `catalog/sources.lock.json`, or record set
`schuss-record-set-000031@1`.

### No parallel collection, implementation, or provider

- `schuss-object-collection-000004@1` remains the function-neutral exact
  56-implementation Mutable-derived discovery cohort. Its record-file SHA-256
  is `df6ac5ee94bd82085a0066d5c5d867cbf1dbec72b9e63c5692de552b5fbe55e2`.
  It has `target_promise: false` and contains no physical package path.
- Task 036 allocated no catalog implementation, component contract, graph,
  binding, eligibility, runtime factory, target/backend pair, operation, or
  stable ID.
- `schuss-implementation-provider-000001@1` remains the only accepted provider
  in this boundary. Its record-file SHA-256 is
  `0a2b36c4e6c9aecf395c1269f8351a7bc0d61ae3a24c3e55946c57879a9c918a`.
  It retains exactly seven Schuss native fixed-Q27 bindings and requires
  `schuss-source-release-000006@1`, not this physical package or the Mutable
  source release.

The exact audit commands passed:

```text
python3 tools/contracts/validate_task033_phase2.py
python3 -m unittest tools.contracts.tests.test_task033_phase2_collection_provider
git diff --exit-code -- catalog contracts/task033/phase2 \
  contracts/record-sets/task033-phase2-collection-provider-v1.json \
  schemas/source-release-v0.schema.json \
  schemas/object-collection-v0.schema.json \
  schemas/implementation-provider-v0.schema.json
```

The validator reported seven source releases, four collections, 140 catalog
implementations, 56 members in the Mutable-derived collection, seven provider
bindings, unchanged parent members, and `native_registry_modified: false`.
All 12 focused collection/provider tests passed.

## Corrected physical package boundary

The first draft of `SOURCE_PACKAGE.json` duplicated the Ksoloti URL/commit and
claimed per-file license review. That conflicted with Task 033 ownership and
was rejected before final removal.

The corrected manifest is generated by
`tools/source_packages/generate_mutable_ksoloti_v1.py` from the live accepted
source-release reference and the physical closure. It contains only:

- the exact source-release reference and schema path;
- the 21 packaged relative paths and their physical byte hashes;
- the path-sensitive closure manifest
  `0903f25038f0116422a8512b15f1c3531e7b22371da8ad393b130a16d821508f`;
- retained notice placement;
- six closed physical component groups; and
- explicit negative claims denying source-release, provenance, licensing,
  collection, catalog implementation, graph, provider, runtime, device,
  real-time, audible, and distribution authority.

It contains no repository URL, commit, branch, source display identity,
per-file license expression, catalog member, implementation ID, provider ID,
target/backend reference, absolute path, hostname, timestamp, or build path.
The retained notice explicitly defers license/distribution truth to
`schuss-source-release-000005@1` and preserves its unreviewed status.
The CMake seam carries no source-release URL, revision, hash, provenance, or
license constants; it validates the generated manifest and lets that manifest
resolve the accepted Task 033 authority before returning physical paths.

Focused authority/closure validation passed:

```text
python3 tools/source_packages/generate_mutable_ksoloti_v1.py --check
python3 tools/source_packages/validate_source_package.py \
  packages/dsp_sources/mutable_ksoloti_v1 \
  --source-root <authenticated-ksoloti-root> \
  --expected-source-release-id schuss-source-release-000005 \
  --expected-source-release-revision 1 \
  --expected-source-release-content-hash \
  sha256:51750a00f07f98c783cfc1972580c9399ac64690eaf3d40ad6e1d736698c972c \
  --expected-closure-manifest-sha256 \
  0903f25038f0116422a8512b15f1c3531e7b22371da8ad393b130a16d821508f
python3 -m unittest tools.source_packages.tests.test_validate_source_package
```

The seven validator tests pass. They cover generation freshness, the absence
of parallel catalog/license metadata, deterministic portable output, modified/
missing/extra bytes, duplicate/unsorted/nonportable/escaping paths, component
closure and unknown components, source-release reference drift, closure hash
drift, absent notices, malformed encoding, unknown fields, and the CMake
unknown-component negative.

The package-owned CMake smoke consumer independently includes representative
stmlib, Braids, and Clouds headers and links the same Braids resource, Clouds
resource, and stmlib units translation units. It has no Tide Pit source/include
path and passed 1/1 CTest.

## Migration, one-copy audit, and recovery

The authority audit above and the first corrected focused validation run were
completed while both 21-file trees were present. Only then was the exact
Tide Pit-local `third_party/ksoloti` subtree moved recoverably outside the
repository. The final inventory is:

- shared `upstream/`: exactly 21 regular files;
- Tide Pit-local `third_party/ksoloti`: zero regular files; and
- Schuss source-tree copies of
  `firmware/mutable_instruments/stmlib/stmlib.h`: exactly one, below the shared
  physical package.

No Tide Pit CMake file, source validator, or code path refers to the old root.
The only retained old-path references are historical migration prose. The
accepted Task 033 records, schemas, record set, and catalog have an empty Git
diff. The external Ksoloti Mutable subtree remained clean at exact commit
`08d3e6e1e2b61230308c20a15ded58ffdaf4656c`; pre-existing unrelated dirty work
in Schuss and the Gills source checkout was preserved.

## Final validation after authority-corrected freeze

All affected checks were rerun after the manifest, validator, CMake seam,
source lock, and duplicate removal froze:

| Boundary | Result |
|---|---|
| Manifest generation and external Ksoloti comparison | fresh and valid; 21 exact files; package manifest SHA-256 `9f45e860f1fff388cc18dab077232196befbc7cfb6f383effd35331bf102f2a1` |
| Source-package negatives | 7/7 unit tests passed |
| Tide Pit source lock | passed with authenticated Gills and Ksoloti roots |
| Independent package CMake consumer | configured, linked three translation units, 1/1 CTest passed |
| Task 033 authority | validator passed; 12/12 collection/provider tests passed; protected diff clean |
| Tide Pit Release | fresh configure/build, 6/6 CTests passed |
| Tide Pit fail-fast ASan/UBSan | fresh configure/build, 6/6 CTests passed with no diagnostic |
| Authenticated JUCE 8.0.15 | archive SHA-256 `04f8d5055382582c757be9da069ea98338005f98248facd9c2804435ac853e70`; fresh build and 8/8 CTests passed |
| Render matrix | 16/64/128/512 partitions, complete snapshots, repeat, overwrite negative, and experiment-drift negative passed within the JUCE CTest |
| Relocated minimal repository | manifest fresh, two validator outputs identical, independent build and 1/1 CTest passed |
| Schuss `current` profile | 4/4 passed; zero failed or incomplete checks |

The canonical Tide Pit comparator remains exactly 1,536,000 bytes with
SHA-256 `39d8c2a67a1b9511b4a063914b01ab816635996a47530e6c09baa8accf45ad2b`,
peak Q27 39,182,832, and RMS Q27 14,011,444.589680206. Upstream `random.cpp`
remains present in the physical closure but excluded from Tide Pit's compiled
source selection. The local `tide_pit_mutable` target retains C++17, Release
`-O2`, and sanitizer instrumentation; `tide_pit_core` retains `BUFSIZE=16` and
`TEST=1`.

The deterministic completion handoff is generated from live repository bytes
and validated by `tools/source_packages/validate_task036_handoff.py`. It binds
the Task 033 source-release reference instead of repeating upstream identity,
revision, provenance, or licensing metadata.

## Corrections made

1. The initial validator used a SHA-256 rule for a 40-character Git commit;
   the schema check was corrected before migration.
2. The shell safety layer rejected permanent recursive deletion. The exact
   duplicate was moved recoverably to Trash instead, then restored when the
   user added the Task 033 authority audit.
3. Raw Markdown line wrapping initially made the retained-notice authority
   sentence fail; the validator now compares normalized whitespace.
4. Most importantly, duplicated upstream/provenance/license metadata was
   removed. The physical manifest is generated from and validated against the
   accepted Task 033 source-release authority.
5. The final Tide Pit source-validator invocation was corrected to its actual
   `--source-root` and `--mutable-root` option names; the corrected command
   passed.
6. The relocated-root fixture initially assumed a nonexistent
   `tools/contracts/__init__.py`. The fixture was corrected to copy the actual
   namespace-package dependency, `validator_core.py`, and then passed.

## Evidence ladder

| Level | Result | Limit |
|---|---|---|
| Source/catalog authority | passed | Task 033 remains authoritative; license and distribution remain unreviewed |
| Physical source closure | passed | exact reusable 21-file build input only |
| Host structural | passed | Release and fail-fast sanitizer 6/6 |
| Host signal | passed | exact comparator and render matrix unchanged |
| Target build | passed | authenticated JUCE renderer, adapter, and standalone compiled and linked |
| Real-time | deferred | no callback deadline, restart, allocation, lock, or underrun promotion |
| Connected device | deferred | no audio/MIDI device or controller opened |
| Listening | deferred | no subjective or A/B listening result |
| Distribution/production | deferred | no packaging, signing, publication, or deployment approval |

## Evidence ceiling

Task 036 may promote the physical source closure, host structure, unchanged
host signal, and authenticated target build only. It creates no provider or
catalog availability and performs no hardware, audio/MIDI device, real-time,
listening, distribution, publication, staging, commit, or push action.
