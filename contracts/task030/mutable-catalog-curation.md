# Task 030 complete Mutable-derived catalog curation

This decision is bounded to the 56 entries carrying
`mutable-instruments-derived` in exact Task 027 source-review revision 1. The
six existing mappings are retained and the remaining 50 receive one exact
catalog implementation each. The 16 unattributed extended entries remain
inventory-only.

## Family reuse

- Elements `string` and `tube` reuse `schuss-family-000010@1`, Physical-model
  Resonator, because both exact source descriptions identify resonating string
  or waveguide bodies within the already reviewed Elements-like function.
- Braids `saw` reuses `schuss-family-000031@1`, Band-limited Saw Oscillator,
  because the exact source description identifies the same ordinary audio saw
  function at a different concrete implementation.
- Every other former candidate receives a new opaque function-first family.
  Source ancestry remains only provenance and alias material.

## New family allocation

Families `000061`-`000070` cover, in source-review order: Topographic Rhythm
Sequencer, Macro Voice, Granular Pitch Shifter, Diffusion Processor, Stereo
Diffusion Reverb, Chorus, Ensemble, Dynamics Control Generator, Vocoder, and
Cross-modulation Processor.

Families `000071`-`000091` cover the Braids-derived entries from Blown Physical
Voice through Question Mark Oscillator. Braids Saw then reuses family `000031`.
Families `000092`-`000107` cover Saw Swarm Voice through Wavetable Voice.
The generator owns the exact closed entry-to-family table and rejects missing,
extra, duplicate, or reordered allocation.

## Implementation allocation

The 50 former tagged candidates are sorted by their retained Task 027 entry
IDs and receive `schuss-implementation-000112@1` through
`schuss-implementation-000161@1` in that order. One entry produces one
implementation. All are `catalogued-only`, compatibility `not-evaluated`, and
retain their exact source limitations.

## Evidence boundary

This is structural/catalog provenance evidence only. Catalog membership does
not establish a component contract, binding, backend eligibility, source
generation, ARM build, connected-device behavior, resource/real-time fitness,
audible behavior, preference, or release readiness. The Warps link failure,
macro-voice source build failure, and Rings-reverb unsupported result remain
unchanged.
