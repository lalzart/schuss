# Catalog overlays

Overlays add reviewed Schuss semantics without rewriting frozen inventory
evidence. Each version binds itself to an exact evidence manifest and keeps
families separate from implementation membership and compatibility evidence.

`phase-4a-semantic-catalog-v0/` is the manually curated pilot for Task 003. It
is not a classification of the complete legacy census.
