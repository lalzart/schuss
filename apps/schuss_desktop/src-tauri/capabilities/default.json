{
  "$schema": "../gen/schemas/desktop-schema.json",
  "description": "Core window capability with no filesystem, shell, network, project-write, build, or hardware plugin permissions.",
  "identifier": "main-read-only-catalog",
  "permissions": [
    "core:default"
  ],
  "windows": [
    "main"
  ]
}
